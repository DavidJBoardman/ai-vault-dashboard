# =====================================================
# Vault Analyser Backend Requirements
# Python 3.12+ (via Conda recommended)
# =====================================================
#
# SETUP INSTRUCTIONS:
#
# 1. Create conda environment:
#    conda create -n vault-interface python=3.12
#    conda activate vault-interface
#
# 2. Install PyTorch first (platform-specific):
#    # macOS (Apple Silicon with MPS):
#    pip install torch torchvision torchaudio
#
#    # Linux/Windows with CUDA 12.6:
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
#
# 3. Install SAM 3 via HuggingFace:
#    pip install git+https://github.com/huggingface/transformers torchvision
#
# 4. Install remaining requirements:
#    pip install -r requirements.txt
#
# =====================================================

# Core API
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
websockets==12.0

# Data handling
numpy==1.26.4
scipy==1.14.1
pandas==2.2.3
pydantic==2.9.2

# Image processing & Gaussian splatting projection
Pillow==10.4.0
scikit-image==0.24.0
opencv-python-headless==4.10.0.84

# Point Cloud (E57, PLY, PCD, XYZ support)
open3d==0.19.0

# Rhino 3DM file support
rhino3dm==8.9.0

# HuggingFace ecosystem (for SAM 3)
# Note: transformers installed via git for latest SAM 3 support
accelerate
safetensors

# Packaging (for production builds)
pyinstaller==6.10.0
